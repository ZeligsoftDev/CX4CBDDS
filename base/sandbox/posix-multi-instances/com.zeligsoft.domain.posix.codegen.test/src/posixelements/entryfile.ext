
import langc;

extension langc::element;
extension langc::expr;
extension langc::name;
extension langc::type;

List testInstanceWithUserAttribute() :
    {
        
    }

    cached Expression storageAddress( uml::Property component, Deployment deployment, DeploymentPart process ) :
;
