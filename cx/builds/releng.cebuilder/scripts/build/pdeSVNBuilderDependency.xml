<?xml version="1.0"?>
<!-- ====================================================================== 
    Properties that must be passed to this script:
    base.install.dir
	dependencyTargets
	local.cache.dir
	dependency.properties
	baseos
	basews
	basearch
                                                               
     ====================================================================== -->
<project name="PDE SVN Builder Dependency" default="get">

	<target name="get">
		<antcall target="getAndInstall">
			<param name="groupId" value="svn-pde-builder" />
		</antcall>
	</target>

	<target name="getAndInstall">
		<echo message="dependency.properties: ${dependency.properties}" />
		<echo message="local.cache.dir: ${local.cache.dir}" />
		<ant antfile="${dependencyTargets}" target="checkDependency">
			<property name="groupId" value="${groupId}" />
		</ant>
		<ant antfile="${dependencyTargets}" target="installDependency">
			<property name="groupId" value="${groupId}" />
			<property name="install.destination" value="${base.install.dir}" />
		</ant>
	</target>
	
</project>

